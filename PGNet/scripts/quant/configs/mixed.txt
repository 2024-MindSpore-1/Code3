[common_quant_param]
# Supports WEIGHT_QUANT, FULL_QUANT or DYNAMIC_QUANT
quant_type=WEIGHT_QUANT

# 设置量化的比特数，目前权重量化支持0-16bit量化，设置为1-16bit时为固定比特量化，设置为0bit时，启用混合比特量化。
# 全量化、动态量化仅支持8bit量化。
bit_num=0

# 设置参与量化的权重尺寸阈值，若权重数大于该值，则对此权重进行量化。
min_quant_weight_size=5000

# 设置参与量化的权重通道数阈值，若权重通道数大于该值，则对此权重进行量化。
min_quant_weight_channel=5

# 设置无需量化的算子名称，多个算子之间用','分割。
#skip_quant_node=node_name1,node_name2,node_name3

# 设置量化Debug信息文件保存的文件夹路径。
debug_info_save_path=scripts/quant/debug/mixed

# 权重量化压缩编码使能开关。若量化的比特数不是 8 或 16，则必须为true。
enable_encode = true

[mixed_bit_weight_quant_param]
# 数值越大可以带来更大的压缩率，但是也会造成不同程度的精度损失
#init_scale=0.02

# 是否自动搜索init_scale参数
auto_tune=true
